function convertCtoF(temp_c) {
    let temp_f = console.log((temp_c * 5/9) + 32);
    let displayMessage = console.log(`A temperature of ${temp_c} degrees Celcius equates to ${temp_f} degrees Fahrenheit.`)
    return displayMessage;
}